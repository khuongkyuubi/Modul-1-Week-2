let inpupWidth = prompt("Enter the width: ");
let inputHeight = prompt("Enter the height: ");
let width = parseInt(inpupWidth);
let height = parseInt(inputHeight);
let area = width * height;
document.write("The area is : " + area);